[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -q --tb=short
markers =
    real_rates: exercises get_rates() itself; opts out of the stubbed-rates fixture
filterwarnings =
    ignore::DeprecationWarning
